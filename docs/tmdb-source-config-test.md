# TMDB 数据源配置测试

配置弹窗使用当前未保存的 API 凭据、API 域名和图片域名执行只读测试。API 检查请求 `3/configuration`，只有成功返回且包含 TMDB `images` 配置对象时通过；图片检查请求固定的小尺寸 TMDB 图片，只有 HTTP 状态�、内容类型、非空响应和图片文件签名均有效时通过。测试在后台线程执行，不保存或改变配置，并分别显示 API 与图片测试结果。
